import { QIO } from './QIO';
export declare class Ref<A> {
    private value;
    static of<A>(a: A): QIO<Ref<A>>;
    private constructor();
    get read(): QIO<A>;
    set(a: A): QIO<A>;
    update(ab: (a: A) => A): QIO<A>;
}
