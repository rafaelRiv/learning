import { IScheduler } from 'ts-scheduler';
import { FiberConfig } from '../internals/FiberConfig';
import { QIO } from '../main/QIO';
import { FiberRuntime } from './FiberRuntime';
export declare class DefaultRuntime extends FiberRuntime {
    readonly scheduler: IScheduler;
    readonly config: FiberConfig;
    constructor(scheduler: IScheduler, config: FiberConfig);
    configure(config: FiberConfig): DefaultRuntime;
    unsafeExecutePromise<A, E>(io: QIO<A, E>): Promise<A>;
}
export declare const defaultRuntime: () => DefaultRuntime;
