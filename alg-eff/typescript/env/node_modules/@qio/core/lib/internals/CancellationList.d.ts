import { LinkedListNode } from 'standard-data-structures';
import { ICancellable } from 'ts-scheduler';
export declare type CancelId = LinkedListNode<ICancellable>;
export declare class CancellationList implements ICancellable {
    private cancelled;
    private readonly Q;
    get isCancelled(): boolean;
    cancel(): void;
    push(cancellable: ICancellable): CancelId;
    remove(id: CancelId): void;
}
