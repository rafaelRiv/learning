"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CancellationList = void 0;
const standard_data_structures_1 = require("standard-data-structures");
class CancellationList {
    constructor() {
        this.cancelled = false;
        this.Q = standard_data_structures_1.DoublyLinkedList.of();
    }
    get isCancelled() {
        return this.cancelled;
    }
    cancel() {
        this.cancelled = true;
        while (this.Q.length > 0) {
            const node = this.Q.pop();
            if (standard_data_structures_1.Option.isSome(node)) {
                node.value.cancel();
            }
        }
    }
    push(cancellable) {
        return this.Q.add(cancellable);
    }
    remove(id) {
        this.Q.remove(id);
    }
}
exports.CancellationList = CancellationList;
